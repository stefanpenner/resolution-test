name: 'L4-08'
description: 'Layer 4 composite 08 (cross-repo)'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-4/leaf-a@main
    - uses: stefanpenner/resolution-shard-1/leaf-b@main
    - uses: stefanpenner/resolution-test/leaf-08@main
      with:
        tag: 'L4-08 → leaf-08'
