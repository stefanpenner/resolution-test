name: 'L4-09'
description: 'Layer 4 composite 09 (cross-repo)'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-5/leaf-a@main
    - uses: stefanpenner/resolution-shard-2/leaf-b@main
    - uses: stefanpenner/resolution-test/leaf-09@main
      with:
        tag: 'L4-09 → leaf-09'
