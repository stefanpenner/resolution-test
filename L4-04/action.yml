name: 'L4-04'
description: 'Layer 4 composite 04 (cross-repo)'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-5/leaf-a@main
    - uses: stefanpenner/resolution-shard-2/leaf-b@main
    - uses: stefanpenner/resolution-test/leaf-04@main
      with:
        tag: 'L4-04 → leaf-04'
