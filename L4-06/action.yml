name: 'L4-06'
description: 'Layer 4 composite 06 (cross-repo)'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-2/leaf-a@main
    - uses: stefanpenner/resolution-shard-4/leaf-b@main
    - uses: stefanpenner/resolution-test/leaf-06@main
      with:
        tag: 'L4-06 → leaf-06'
