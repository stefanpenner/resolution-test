name: 'L4-10'
description: 'Layer 4 composite 10 (cross-repo)'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-1/leaf-a@main
    - uses: stefanpenner/resolution-shard-3/leaf-b@main
    - uses: stefanpenner/resolution-test/leaf-10@main
      with:
        tag: 'L4-10 → leaf-10'
