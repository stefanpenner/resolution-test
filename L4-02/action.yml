name: 'L4-02'
description: 'Layer 4 composite 02 (cross-repo)'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-3/leaf-a@main
    - uses: stefanpenner/resolution-shard-5/leaf-b@main
    - uses: stefanpenner/resolution-test/leaf-02@main
      with:
        tag: 'L4-02 → leaf-02'
