name: 'L4-02'
description: 'Layer 4 composite - distributed across repos'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-3/leaf@main
    - uses: stefanpenner/resolution-shard-7/leaf@main
    - uses: stefanpenner/resolution-shard-2/leaf@main
