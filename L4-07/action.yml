name: 'L4-07'
description: 'Layer 4 composite 07 (cross-repo)'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-3/leaf-a@main
    - uses: stefanpenner/resolution-shard-5/leaf-b@main
    - uses: stefanpenner/resolution-test/leaf-07@main
      with:
        tag: 'L4-07 → leaf-07'
