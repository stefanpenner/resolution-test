name: 'L4-03'
description: 'Layer 4 composite 03 (cross-repo)'
runs:
  using: 'composite'
  steps:
    - uses: stefanpenner/resolution-shard-4/leaf-a@main
    - uses: stefanpenner/resolution-shard-1/leaf-b@main
    - uses: stefanpenner/resolution-test/leaf-03@main
      with:
        tag: 'L4-03 → leaf-03'
